'use strict';

describe('Controller: owner-start-pageController', function() {

    beforeEach(module('core'));

    var owner-start-pageController,
        scope;

    beforeEach(inject(function($controller, $rootScope) {
        scope = $rootScope.$new();
        owner-start-pageController = $controller('owner-start-pageController', {
        $scope: scope
        });
    }));

    it('should ...', function() {

    });
});
